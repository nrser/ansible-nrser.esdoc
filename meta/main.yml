---
# meta file for nrser.esdoc

allow_duplicates: yes

dependencies: # []
- role: nrser.state_mate

- role: qb.gitignore
  gitignore_dest: "{{ esdoc_package_dir }}"
  gitignore_tag: esdoc generated output
  gitignore_content: "/{{ (esdoc_package_dir + '/' + esdoc_destination) | realpath | relpath(esdoc_package_dir) }}"
